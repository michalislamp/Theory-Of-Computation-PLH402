#include <math.h>
#include <stdio.h>
#include "lambdalib.h"

typedef struct Coordinates {
    double latitude, longitude;

} Coordinates;

int foo(int x, double y[22], char* alpha, int z[15]) {

    a = readstr();

    if(x < y[1]) {
        return x;
    } else {
        return (x - 1);
    }
}

int main() {

    const double pi = 3.14152;

    Coordinates w, z;

    int a, b;

    a = 2;

    boolean array[15];

    for(int i=10; i<-1; i=i+100) {

        if(a == 2) {
            b = foo(x, y, z);
            a += b;
        }
    }

    a = 0;

    while(a < 5) {

        if(a == 0) {
            break;
            }
        a += 1;
    }

}